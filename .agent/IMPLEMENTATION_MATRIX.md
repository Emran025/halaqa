# مصفوفة تنفيذ عميل Halaqa WPF

## قاعدة الانتقال

لا تعد أي ميزة مكتملة لمجرد ظهور واجهتها. تنتقل بطاقة الميزة من `Planned` إلى `Implemented` فقط عندما تحتوي `Presentation` و`Domain` و`Data`، وتطابق DTOs مسار ومخطط OpenAPI، وتنجح اختبارات التحويل وحالات الاستخدام، وتنجح عملية البناء على Windows. تنتقل إلى `Verified` بعد اختبار تدفق ناجح وفشل 401/403/404/409/422 وفشل الشبكة عند انطباقه.

| الرمز | الميزة | مسارات العقد الرئيسية | الوضع الحالي | بوابة الاختبار قبل الدمج |
|---|---|---|---|---|
| SH-00 | النظام المشترك والثيم | غير منطبق | In progress | بناء WPF، اختبار الموارد، فحص RTL والتنقل والوصول. |
| AU-01 | الدخول والخروج والجلسة | `/auth/login`, `/auth/logout`, `/me` | Partial | DTO/login/use case/session store واختبار 401 و422 والخروج. |
| AU-02 | التسجيل واستعادة كلمة المرور | `/auth/register/*`, `/auth/password/*`, `/auth/password/change` | Planned | تحقق متعدد الخطوات، idempotency، رسائل الأخطاء الحقلية، تدفق النجاح. |
| AC-01 | الملفات التفصيلية والوثائق | `/me/student-profile`, `/me/teacher-profile`, `/me/teacher-documents` | Implemented | نُفذت ملفات الطالب والمعلم ووثائق المعلم عبر طبقات Domain/Data/Presentation، ونجحت بوابات Windows CI؛ ما زال اختبار API الحي لسلوك الخصوصية و403/422 ضمن التحقق اللاحق. |
| AC-02 | الملف العام | `GET /me`, `PATCH /me` | Implemented | DTO/mapper، تحديث جزئي آمن، حالات استخدام، واجهة RTL، واختبارات Windows CI؛ لا يشمل اختبارات API حيّة أو الملفات التفصيلية. |
| HA-01 | الحلقات والعضويات | `/halaqas`, عضوياتها وطلابها | Partial | نُفذت إدارة حلقات المعلم وعميل العضويات (القائمة المرقمة، الإسناد، تغيير الحالة والإزالة) عبر طبقات Domain/Data/Presentation ونجحت بوابات Windows CI. يعتمد عميل العضويات على مسار `GET /halaqas/{id}/memberships` المقترح؛ يلزم التحقق الحي بعد دمج الخلفية، كما تبقى واجهة اختيار الطلبات المقبولة واختبارات 403/409 الحية لاحقة. |
| RG-01 | الطلبات والمعلمون | `/teachers`, `/student-applications`, `/registration-requests` | Partial | نُفذت مراجعة المعلم لطلبات التسجيل الواردة إلى الحلقة (قائمة مرقمة وفلترة وقبول ورفض وطلب استكمال)، ومسار الطالب لتصفح بطاقات المعلمين والحلقات العامة وإرسال طلب موجّه من لقطة ملفه الرسمية في الذاكرة مع `client_operation_id`، وشاشة طلباته المرقمة مع فلترة الحالة وسحب الطلب المعلّق وإعادة التحميل من الخادم. نجحت بوابات Windows CI، ولا تعرض الواجهات قبل القبول إلا البيانات العامة المسموح بها. تبقى شاشة صندوق الطلبات العام للمعلم، والتحقق الحي من 403/409/422 وعمليات الخادم الفعلية لاحقة. |
| FU-01 | الخطة والحضور والمتابعة | `/students/{id}/follow-up-plan`, availability, `/follow-up-items`, trackings | Planned | تكرار الخطة، timezone، complete/skip/reschedule، 409/422. |
| QU-01 | المصحف المحلي والرسمي | `/quran/*`, `/sessions/{id}/mushaf-state` | Partial | يقرأ المصدر المحلي read-only من `QuranV3.sqlite` النص المقروء `AyaDiac` ومسار العرض المرّمز `Uthomanic_text` مع تحويل `\\n` الحرفي إلى سطر فعلي. أُدرجت خطوط QCF المرجعية الـ604 (`p2001.ttf`–`p2604.ttf`) ويُختار خط الصفحة المطابق في WPF، لذلك دقة العرض بالرموز الصفحية متاحة للمصدر المحلي فقط. بديل المصدر البعيد يحتفظ بنص التوافق ولا يعدّ مطابقاً للرموز الصفحية ما لم يوفر العقد حقلاً للرموز. نجحت بوابة Windows CI؛ لم يُنفذ تحقق بصري يدوي على Windows بعد، ولا يزال اختبار API الحي لـ cache failure والحفظ الرسمي و422/409 لاحقاً. |
| SE-01 | الجلسات والمهام | `/sessions`, realtime, tasks, draft | Partial | نُفذت واجهة RTL للجلسة بأقسام الوسائط المباشرة والمهمة والحضور المؤقت والتسجيل المحلي، وعارض مصحف محلي يستخدم الرموز `Uthomanic_text` مع خط QCF خاص بالصفحة لكل واحدة من الصفحات الـ604، وتنقل الصفحات واختيار الآية ومتابعة الطرف الآخر وفصل الحضور P2P المؤقت عن التثبيت الرسمي. نجحت بوابة Windows CI؛ لا يشمل ذلك تحققاً بصرياً يدوياً ولا اختياراً بالنقر لكل رمز داخل الصفحة. لا يزال محول WebRTC/Media الفعلي المتوافق مع Windows وHost ICE فقط، وتفويض/إشارة WebSocket والتحقق الحي وإدارة المهام والتقارير، أعمالاً لاحقة؛ لا وسائط عبر الخادم. |
| MI-01 | الأخطاء والملاحظات والتقييم | mistakes, notes, evaluation | Partial | SQLite outbox، منع تكرار منطقي، CRUD و409/422، مزامنة. |
| RP-01 | التقرير والتقدم والسجل | report, progress, reports, historical mistakes | Planned | اعتماد الطرفين، reopen، pagination، صلاحيات العلاقة التعليمية. |
| NO-01 | الإشعارات | `/notifications`, read, read-all | Planned | قائمة المستخدم فقط، unread/read-all، empty/error states. |

## سياسة الفروع والمراجعة

لكل بطاقة فرع باسم `feat/<code>-<slug>` ينطلق من `main`. يضيف الفرع تنفيذ طبقات الميزة والاختبارات وملاحظة في هذه المصفوفة. يفتح Pull Request إلى `main` بعد نجاح GitHub Actions. تراجع الفروق بمقارنة المسارات وDTOs مع `quran-halaqa/.agent/openapi.yaml`، ثم يدمج Pull Request فقط إذا كانت الفحوص خضراء ولا توجد تعارضات أو أسرار أو انحراف عن معمارية الطبقات.

## تسلسل التنفيذ المعتمد

1. SH-00 ثم AU-01 وAU-02 لأن بقية الرحلات تحتاج هوية وجلسة وإدارة أخطاء موحدة.
2. AC-01 ثم HA-01 وRG-01 لأن تكوين العلاقة التعليمية يسبق المتابعة.
3. FU-01 ثم QU-01 وMI-01 لأن واجهة الجلسة تحتاج مهمة ونطاقاً وسجل أخطاء.
4. SE-01 ثم RP-01 وNO-01.

لا يغير العميل عقد API ولا يضيف endpoint مفترضاً. إذا ظهر تعارض بين تجربة المستخدم والعقد، يسجل كفجوة عقدية في Pull Request ولا يموه بحالة محلية دائمة.
