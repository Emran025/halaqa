# مصفوفة تنفيذ عميل Halaqa WPF

## قاعدة الانتقال

لا تعد أي ميزة مكتملة لمجرد ظهور واجهتها. تنتقل بطاقة الميزة من `Planned` إلى `Implemented` فقط عندما تحتوي `Presentation` و`Domain` و`Data`، وتطابق DTOs مسار ومخطط OpenAPI، وتنجح اختبارات التحويل وحالات الاستخدام، وتنجح عملية البناء على Windows. تنتقل إلى `Verified` بعد اختبار تدفق ناجح وفشل 401/403/404/409/422 وفشل الشبكة عند انطباقه.

| الرمز | الميزة | مسارات العقد الرئيسية | الوضع الحالي | بوابة الاختبار قبل الدمج |
|---|---|---|---|---|
| SH-00 | النظام المشترك والثيم | غير منطبق | In progress | بناء WPF، اختبار الموارد، فحص RTL والتنقل والوصول. |
| AU-01 | الدخول والخروج والجلسة | `/auth/login`, `/auth/logout` | Partial | توجد طبقات Auth المسجلة وتدفق الدخول/الخروج واستعادة الجلسة من الغلاف، مع اختبارات حالات استخدام. يلزم تحقق API حي من 401 و422 والخروج واستعادة الرمز؛ لا يعني ذلك اكتمال التسجيل أو إثبات التحقق الحي لمسارات الحساب الأخرى. |
| AU-02 | التسجيل واستعادة كلمة المرور | `/auth/register/*`, `/auth/password/*`, `/auth/password/change` | Partial | توجد طبقات Data/Domain/Presentation مسجلة لتسجيل الطالب والمعلم وطلب/إعادة ضبط كلمة المرور، وتصل إليها شاشة الدخول. تغيير كلمة المرور صار رحلة مصادقاً عليها من لوحة المستخدم، مع تهيئة تمسح الإدخال الحساس ورسائل حقول `current_password` و`password` و`password_confirmation` وزر عودة، واختبارات ViewModel/لوحة. يلزم تحقق API حي من 401 و409/422 ورسائل الحقول وحالات النجاح؛ لا يفترض العميل سياسة إبطال الجلسة لأنها غير معلنة في الاستجابة 204. |
| AC-01 | الملفات التفصيلية والوثائق | `/me/student-profile`, `/me/teacher-profile`, `/me/teacher-documents` | Partial | توجد طبقات Domain/Data/Presentation وتسجيل DI ووصول من لوحة الدور، ورفع وثائق المعلم يستعمل multipart. توجد اختبارات mapper/use case، لكن لا يوجد تحقق API حي موثق للصلاحية 403 أو 422 أو دورة رفع/عرض/حذف الوثيقة؛ لذلك لا تعد الرحلة مكتملة. |
| AC-02 | الملف العام | `GET /me`, `PATCH /me` | Partial | توجد طبقات Data/Domain/Presentation وتسجيل DI واختبارات mapper/use case، وصارت لوحة التطبيق تعرض بطاقة بيانات عامة مستقلة تطلق `ProfileRequested` وتصل إلى `GeneralProfileViewModel` المسجل، مع بقاء ملف الطالب أو المعلم المتخصص منفصلاً. يلزم Windows CI والتحقق الحي من 422 ورسائل الحقول ودورة تحديث البيانات. |
| HA-01 | الحلقات والعضويات | `/halaqas`, `/halaqas/{id}/students`, `/halaqas/{id}/memberships/{membershipId}` | Partial | إدارة الحلقات والعضويات لها طبقات Data/Domain/Presentation مسجلة ووصول من لوحة المعلم واختبارات mapper/use case. **فجوة عقد:** عميل القائمة يستهلك `GET /halaqas/{id}/memberships?page=&status=`، لكن OpenAPI الحالي يعلن فقط `GET /halaqas/{id}/students` بمرشحات `search` و`status` و`page` و`per_page` واستجابة طلاب. لا يعد مسار القائمة عاملاً أو مطابقاً حتى ينسق في Laravel أو يموه العميل بعد تحديث العقد؛ تبقى اختبارات 403/404/409 الحية لاحقة. |
| RG-01 | الطلبات والمعلمون | `/teachers`, `/registration-requests`, `/halaqas/{id}/registration-requests`, `/student-applications` | Partial | توجد طبقات Data/Domain/Presentation مسجلة لعرض المعلمين وإرسال الطلب الموجه وطلبات الطالب وطلبات الحلقة وصندوق المعلم العام مع قبول/رفض/طلب استكمال/سحب، ومسارات وصول من الغلاف. صندوق المعلم العام يستهلك `applicants` والبحث والحالة والترقيم المعلنة، ويعرض الملخص العام فقط قبل القبول؛ لا يفتح الملف التفصيلي ولا ينفذ إجراء حمولة تلقائياً. يلزم Windows CI والتحقق الحي للخصوصية قبل القبول و403/404/409/422؛ لا تدعي الواجهة تنفيذ عمليات الخادم حياً. |
| FU-01 | الخطة والحضور والمتابعة | `/students/{studentId}/follow-up-plan`, `/students/{studentId}/availability`, `/follow-up-items`, `/students/{studentId}/trackings` | Partial | نُفذت طبقات Data/Domain/Presentation مسجلة لخطة الطالب وأوقات حضوره وعناصر المتابعة وسجل الحضور، مع PUT للخطة/الحضور وعمليات complete/skip/reschedule و`client_operation_id` واختبارات mapper/ViewModel. تصل رحلة الطالب من لوحة التطبيق بهويته فقط؛ لا تتيح اختيار طالب للمعلم حتى تتوفر قائمة طلاب مطابقة للعقد، وتحرر الواجهة الآن تفاصيل الخطة وفترات الحضور المتعددة مع منع حذف العنصر الوحيد والتحقق من قيود العقد قبل PUT. يلزم Windows CI والتحقق الحي من 403/409/422 وحفظ القوائم المتعددة. |
| QU-01 | المصحف المحلي والرسمي | `GET /quran/pages/{pageNumber}`, `PUT /sessions/{id}/mushaf-state` | Partial | يوجد مصدر SQLite read-only ومصدر صفحة بعيد وrepository وحالة استخدام مسجلة، وقارئ مستقل مسجل في DI ويمكن فتحه من لوحة التطبيق. يعرض القارئ المحلي `Uthomanic_text` بخط QCF الصفحي الصحيح مع ملاحة 1–604 وقائمة الآيات، ويوضح صراحةً أن بديل الخادم نص توافق لأن OpenAPI لا يعلن رمزاً صفحياً في `Ayah`. لا يحفظ القارئ حالة جلسة ولا يشارك موضعاً؛ يلزم Windows CI واختبار SQLite/cache و404 و422/409 الحي. |
| SE-01 | الجلسات والمهام | `GET /sessions`, `GET/POST /sessions/{id}/tasks`, `GET /sessions/{id}/realtime`, `POST /realtime/channels/authorize`, `PUT /sessions/{id}/mushaf-state` | Partial | توجد قائمة جلسات رسمية مرقمة ومفلترة بالحالة والتاريخ، مسجلة في DI ويمكن فتحها من لوحة التطبيق لكلا الدورين؛ تعرض فقط الحقول المعلنة و`direct_p2p_only`. يمكن اختيار الجلسة وقراءة مهامها الرسمية وحالتها وكمياتها وتقييماتها. تتيح واجهة المعلم إنشاء المهمة مع الحقول الاختيارية المعلنة (`sequence_no` والكمية ووحدات النطاق وصفحاته وآياته)، وتحديث الحقول المعلنة في `PATCH /sessions/{id}/tasks/{taskId}` (النطاق والموضع والحالة والكميات) مع إرسال الحقول المدخلة فقط وإعادة تحميل الخادم. لا تعرّض الواجهة عملية التعديل للطالب لأن العقد يوثق موضعه «المسموح» بلا شرط حالة قابل للتنفيذ في العميل. يمكن للطرفين عرض تقييمَي المعلم والطالب للمهمة المختارة وحفظ تقييمهما الخاص (درجة 0–100 وملاحظة حتى 2000 حرف) عبر GET/PUT الرسميين، مع عرض الاستجابة الراجعة من الخادم؛ لا توجد إدارة لتاريخ التقييمات. يبقى `LiveSessionViewModel` غير موصول بقائمة الجلسات، وتبقى `IPeerMediaConnection` و`IMushafRealtimeChannel` و`ILocalVideoRecorder` و`IRealtimeSignalingClient` عقوداً بلا فئات تنفيذ أو WebSocket/WebRTC أو تسجيل. يمكن للطرفين المصرح لهما حفظ مسودة للمهمة المختارة عبر العملية المعلنة مع `client_operation_id` وموضع اختياري ضمن الحدود، ثم يعاد تحميل الخادم؛ لا نقل مباشر عامل ولا وسائط عبر الخادم أو relay أو STUN/TURN. |
| MI-01 | الأخطاء والملاحظات والتقييم | `POST /sessions/{id}/tasks/{taskId}/mistakes` | Partial | يوجد outbox محلي وعميل HTTP وrepository وخدمة مزامنة وحالة استخدام للأخطاء، مع واجهة RTL مسجلة تصل من المهمة المختارة للطرفين المصرح لهما. تتحقق الواجهة من موضع الآية/الصفحة/الكلمة وتعرض نتيجة العملية الفعلية: تمت المزامنة أو انتظار الاتصال أو تعارض أو فشل؛ لا تدعي تسليماً فورياً عندما يحتفظ outbox بالعملية. توجد واجهة RTL لملاحظات المهمة من المهمة المختارة: تعرض قائمة الخادم، وتنشئ ملاحظة بنص من 1 إلى 2000 حرف و`client_operation_id`، وتسمح في العميل بتعديل أو حذف ملاحظة المؤلف الحالي فقط بصورة محافظة؛ يبقى الخادم صاحب صلاحية 403/404/409 النهائية. لا يصح ادعاء اختبار 403/404/409/422 حي قبل التحقق. |
| RP-01 | التقدم والتقرير والسجل | `GET /students/{studentId}/progress`، reports، historical mistakes | Partial | توجد لوحة RTL للتقدم الشخصي تصل من حساب الطالب الحالي فقط، وتعرض إجماليات الجلسات والمهام والأخطاء وآخر نطاقات الحفظ/المراجعة/التلاوة، مع مرشح `task_type` المعلن. لا تعرض الواجهة تقدماً لطالب آخر ولا تتيح كتابة بيانات التقدم. يبقى سجل التقارير والأخطاء التاريخية غير منفذَين حتى يراجع كل مسار وحقوله وترقيمه بصورة مستقلة؛ لا يوجد تحقق API حي موثق. |
| NO-01 | الإشعارات | `/notifications`, `POST /notifications/{notificationId}/read`, `POST /notifications/read-all` | Partial | نُفذت طبقات Data/Domain/Presentation مسجلة لقائمة المستخدم المرقمة وتصفية غير المقروء وتعليم إشعار واحد أو الجميع كمقروء، مع إعادة التحميل من الخادم بعد 204 واختبارات mapper/ViewModel ووصول من لوحة التطبيق. تعرض الواجهة حمولة الإجراء فقط ولا تنفذ `action_path` أو تقبل/ترفض/تنضم تلقائياً؛ يلزم Windows CI والتحقق الحي من 404 وقائمة فارغة وترقيم وصلاحية الحساب. |

## سياسة الفروع والمراجعة

لكل بطاقة فرع باسم `feat/<code>-<slug>` ينطلق من `main`. يضيف الفرع تنفيذ طبقات الميزة والاختبارات وملاحظة في هذه المصفوفة. يفتح Pull Request إلى `main` بعد نجاح GitHub Actions. تراجع الفروق بمقارنة المسارات وDTOs مع `quran-halaqa/.agent/openapi.yaml`، ثم يدمج Pull Request فقط إذا كانت الفحوص خضراء ولا توجد تعارضات أو أسرار أو انحراف عن معمارية الطبقات.

## تسلسل التنفيذ المعتمد

1. SH-00 ثم AU-01 وAU-02 لأن بقية الرحلات تحتاج هوية وجلسة وإدارة أخطاء موحدة.
2. AC-01 ثم HA-01 وRG-01 لأن تكوين العلاقة التعليمية يسبق المتابعة.
3. FU-01 ثم QU-01 وMI-01 لأن واجهة الجلسة تحتاج مهمة ونطاقاً وسجل أخطاء.
4. SE-01 ثم RP-01 وNO-01.

لا يغير العميل عقد API ولا يضيف endpoint مفترضاً. إذا ظهر تعارض بين تجربة المستخدم والعقد، يسجل كفجوة عقدية في Pull Request ولا يموه بحالة محلية دائمة.
