<UserControl x:Class="Halaqa.Desktop.Features.Progress.Presentation.Views.StudentProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Halaqa.Desktop.Shared.Presentation.Controls"
             FlowDirection="RightToLeft">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="28,24" MaxWidth="820">
            <DockPanel Margin="0,0,0,20">
                <Button Command="{Binding BackCommand}" DockPanel.Dock="Left" Style="{StaticResource AppBackButton}" ToolTip="العودة إلى لوحة التحكم">
                    <controls:AppIcon Kind="ArrowRight" />
                </Button>
                <StackPanel>
                    <TextBlock Text="تقدمي وتقاريري" Style="{StaticResource PageTitle}" />
                    <TextBlock Text="يعرض هذا المسار بيانات حساب الطالب الحالي كما يعيدها الخادم." Style="{StaticResource BodyMuted}" />
                </StackPanel>
            </DockPanel>
            <Border Background="{StaticResource AppSurfaceBrush}" Padding="16" CornerRadius="4" Margin="0,0,0,16">
                <DockPanel>
                    <ComboBox ItemsSource="{Binding TaskTypeOptions}" SelectedValue="{Binding SelectedTaskType}" SelectedValuePath="Value" DisplayMemberPath="Label" Width="190" DockPanel.Dock="Right" ToolTip="نوع المهمة" />
                    <Button Content="تحديث التقدم" Command="{Binding LoadCommand}" DockPanel.Dock="Left" Style="{StaticResource AppOutlinedButton}" />
                </DockPanel>
            </Border>
            <UniformGrid Columns="3" Margin="0,0,0,16">
                <Border Background="{StaticResource AppSurfaceBrush}" Padding="16" Margin="4" CornerRadius="4"><StackPanel><TextBlock Text="الجلسات" Style="{StaticResource BodyMuted}"/><TextBlock Text="{Binding Progress.Totals.TotalSessions}" Style="{StaticResource PageTitle}"/></StackPanel></Border>
                <Border Background="{StaticResource AppSurfaceBrush}" Padding="16" Margin="4" CornerRadius="4"><StackPanel><TextBlock Text="المهام" Style="{StaticResource BodyMuted}"/><TextBlock Text="{Binding Progress.Totals.TotalTasks}" Style="{StaticResource PageTitle}"/></StackPanel></Border>
                <Border Background="{StaticResource AppSurfaceBrush}" Padding="16" Margin="4" CornerRadius="4"><StackPanel><TextBlock Text="الأخطاء" Style="{StaticResource BodyMuted}"/><TextBlock Text="{Binding Progress.Totals.TotalMistakes}" Style="{StaticResource PageTitle}"/></StackPanel></Border>
            </UniformGrid>
            <Border Background="{StaticResource AppSurfaceBrush}" Padding="16" CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="آخر نطاقات مكتملة" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="{Binding Progress.LastCompleted.Memorization.StartPage, StringFormat=الحفظ: يبدأ من الصفحة {0}}" Margin="0,10,0,0" />
                    <TextBlock Text="{Binding Progress.LastCompleted.Review.StartPage, StringFormat=المراجعة: يبدأ من الصفحة {0}}" Margin="0,6,0,0" />
                    <TextBlock Text="{Binding Progress.LastCompleted.Recitation.StartPage, StringFormat=التلاوة: يبدأ من الصفحة {0}}" Margin="0,6,0,0" />
                </StackPanel>
            </Border>
            <TextBlock Text="{Binding Message}" Foreground="{StaticResource AppInfoBrush}" TextWrapping="Wrap" Margin="0,16,0,0" />
            <ProgressBar IsIndeterminate="True" Height="3" Margin="0,12,0,0" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
