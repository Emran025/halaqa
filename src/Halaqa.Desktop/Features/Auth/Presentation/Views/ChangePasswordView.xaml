<UserControl x:Class="Halaqa.Desktop.Features.Auth.Presentation.Views.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Halaqa.Desktop.Shared.Presentation.Controls"
             FlowDirection="RightToLeft">
    <Grid Background="{StaticResource AppCanvasBrush}">
        <Border Style="{StaticResource AppCard}" Width="520" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <controls:AppIcon Kind="LockCheck" Width="42" Height="42" Foreground="{StaticResource AppPrimaryBrush}"  />
                <TextBlock Text="تغيير كلمة المرور" Style="{StaticResource PageTitle}" Margin="0,12,0,4" />
                <TextBlock Text="استخدم كلمة مرور قوية وفريدة لحماية حسابك. لا تُعرض كلمات المرور أو تُخزّن محلياً." Style="{StaticResource BodyMuted}" Margin="0,0,0,16" TextWrapping="Wrap" />
                <PasswordBox x:Name="CurrentPasswordInput" PasswordChanged="PasswordChanged" ToolTip="كلمة المرور الحالية" />
                <TextBlock Text="{Binding CurrentPasswordError}" Foreground="{StaticResource AppErrorBrush}" TextWrapping="Wrap" Margin="0,-6,0,8" />
                <PasswordBox x:Name="PasswordInput" PasswordChanged="PasswordChanged" ToolTip="كلمة المرور الجديدة" />
                <TextBlock Text="{Binding PasswordError}" Foreground="{StaticResource AppErrorBrush}" TextWrapping="Wrap" Margin="0,-6,0,8" />
                <PasswordBox x:Name="PasswordConfirmationInput" PasswordChanged="PasswordChanged" ToolTip="تأكيد كلمة المرور الجديدة" />
                <TextBlock Text="{Binding PasswordConfirmationError}" Foreground="{StaticResource AppErrorBrush}" TextWrapping="Wrap" Margin="0,-6,0,8" />
                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="0,8,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource BodyMuted}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsError}" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource AppErrorBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="رجوع" Command="{Binding BackCommand}" Style="{StaticResource AppSecondaryButton}" />
                    <Button Content="تغيير كلمة المرور" Command="{Binding SubmitCommand}" />
                </StackPanel>
                <ProgressBar IsIndeterminate="True" Height="3" Margin="0,8,0,0" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
