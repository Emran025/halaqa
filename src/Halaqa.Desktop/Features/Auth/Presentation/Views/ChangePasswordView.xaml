<UserControl x:Class="Halaqa.Desktop.Features.Auth.Presentation.Views.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Halaqa.Desktop.Shared.Presentation.Controls"
             FlowDirection="RightToLeft">
    <Grid Background="{StaticResource AppCanvasBrush}">
        <Border Style="{StaticResource AppCard}" Width="480" Padding="32,28" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <Border Width="48" Height="48" CornerRadius="24" Background="#141B524A" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <controls:AppIcon Kind="LockCheck" Width="26" Height="26" Foreground="{StaticResource AppPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <TextBlock Text="تغيير كلمة المرور" Style="{StaticResource PageTitle}" HorizontalAlignment="Center" Margin="0,0,0,6" />
                <TextBlock Text="استخدم كلمة مرور قوية لحماية حسابك (8 خانات على الأقل)." Style="{StaticResource BodyMuted}" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,24" TextWrapping="Wrap" />

                <!-- Current Password -->
                <TextBlock Text="كلمة المرور الحالية" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource AppInkBrush}" Margin="0,0,0,6" />
                <PasswordBox x:Name="CurrentPasswordInput" PasswordChanged="PasswordChanged" Height="42" Padding="10,0" />
                <TextBlock Text="{Binding CurrentPasswordError}" Foreground="{StaticResource AppErrorBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,12" />

                <!-- New Password -->
                <TextBlock Text="كلمة المرور الجديدة" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource AppInkBrush}" Margin="0,0,0,6" />
                <PasswordBox x:Name="PasswordInput" PasswordChanged="PasswordChanged" Height="42" Padding="10,0" />
                <TextBlock Text="{Binding PasswordError}" Foreground="{StaticResource AppErrorBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,12" />

                <!-- Confirm Password -->
                <TextBlock Text="تأكيد كلمة المرور الجديدة" FontWeight="SemiBold" FontSize="13" Foreground="{StaticResource AppInkBrush}" Margin="0,0,0,6" />
                <PasswordBox x:Name="PasswordConfirmationInput" PasswordChanged="PasswordChanged" Height="42" Padding="10,0" />
                <TextBlock Text="{Binding PasswordConfirmationError}" Foreground="{StaticResource AppErrorBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,4,0,12" />

                <!-- Status / Feedback Message -->
                <Border Background="#14DC3545" BorderBrush="#33DC3545" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,0,14">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsError}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding Message}" Foreground="{StaticResource AppErrorBrush}" FontSize="12" TextWrapping="Wrap" />
                </Border>

                <!-- Actions -->
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="رجوع" Command="{Binding BackCommand}" Style="{StaticResource AppSecondaryButton}" Height="40" />
                    <Button Grid.Column="2" Content="تغيير كلمة المرور" Command="{Binding SubmitCommand}" Style="{StaticResource AppPrimaryButton}" Height="40" Padding="20,0" />
                </Grid>
                <ProgressBar IsIndeterminate="True" Height="3" Margin="0,12,0,0" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
