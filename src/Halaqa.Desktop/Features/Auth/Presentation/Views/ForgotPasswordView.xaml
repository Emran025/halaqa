<UserControl x:Class="Halaqa.Desktop.Features.Auth.Presentation.Views.ForgotPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Halaqa.Desktop.Shared.Presentation.Controls"
             FlowDirection="RightToLeft">
    <Grid Background="{StaticResource AppCanvasBrush}">
        <Border Style="{StaticResource AppCard}" Width="460" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <controls:AppIcon Kind="LockReset" Width="42" Height="42" Foreground="{StaticResource AppPrimaryBrush}"  />
                <TextBlock Text="استعادة كلمة المرور" Style="{StaticResource PageTitle}" Margin="0,12,0,4" />
                <TextBlock Text="أدخل بريدك الإلكتروني لإرسال تعليمات إعادة التعيين بصورة آمنة." Style="{StaticResource BodyMuted}" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" ToolTip="البريد الإلكتروني" Margin="0,18,0,8" />
                <TextBlock Text="{Binding Message}" Style="{StaticResource BodyMuted}" />
                <Button Command="{Binding SubmitCommand}" Margin="0,12,0,0">
                    <TextBlock Text="إرسال التعليمات" Foreground="White" />
                </Button>
                <Button Content="لدي رمز إعادة التعيين" Command="{Binding OpenResetCommand}" Style="{StaticResource AppSecondaryButton}" Margin="0,8,0,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
