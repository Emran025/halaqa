<UserControl x:Class="Halaqa.Desktop.Features.Auth.Presentation.Views.ForgotPasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Halaqa.Desktop.Shared.Presentation.Controls"
             FlowDirection="RightToLeft">
    <Grid Background="{StaticResource AppCanvasBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Border Style="{StaticResource AppElevatedCard}" MaxWidth="500" Margin="28" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <Border Width="48" Height="48" CornerRadius="4" Background="{StaticResource AppPrimarySoftBrush}" HorizontalAlignment="Right">
                        <controls:AppIcon Kind="LockReset" Foreground="{StaticResource AppPrimaryBrush}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="استعادة كلمة المرور" Style="{StaticResource PageTitle}" Margin="0,18,0,4" />
                    <TextBlock Text="أدخل بريدك الإلكتروني وسنرسل تعليمات إعادة التعيين بصورة آمنة." Style="{StaticResource BodyMuted}" Margin="0,0,0,20" />
                    <TextBlock Text="البريد الإلكتروني" Style="{StaticResource CaptionText}" />
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" FlowDirection="LeftToRight" ToolTip="name@example.com" />
                    <Border Background="{StaticResource AppInfoSoftBrush}" BorderBrush="#332367C9" BorderThickness="1" CornerRadius="3" Padding="12" Margin="0,0,0,8">
                        <TextBlock Text="{Binding Message}" Style="{StaticResource BodyMuted}" />
                    </Border>
                    <Button Command="{Binding SubmitCommand}" HorizontalAlignment="Stretch" Margin="0,10,0,4"><TextBlock Text="إرسال التعليمات" Foreground="White" /></Button>
                    <Button Content="لدي رمز إعادة التعيين" Command="{Binding OpenResetCommand}" Style="{StaticResource AppSecondaryButton}" HorizontalAlignment="Right" />
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
