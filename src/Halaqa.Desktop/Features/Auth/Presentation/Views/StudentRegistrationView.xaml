<UserControl x:Class="Halaqa.Desktop.Features.Auth.Presentation.Views.StudentRegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Halaqa.Desktop.Shared.Presentation.Controls"
             FlowDirection="RightToLeft">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Background="{StaticResource AppCanvasBrush}">
        <Border Style="{StaticResource AppCard}" Width="760" MaxHeight="720" HorizontalAlignment="Center" VerticalAlignment="Center">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="إنشاء حساب طالب" Style="{StaticResource PageTitle}" />
                    <TextBlock Text="أكمل خطوات التسجيل ثم راجع طلبك قبل إرساله." Style="{StaticResource BodyMuted}" Margin="0,4,0,16" />
                    <ProgressBar Minimum="1" Maximum="3" Value="{Binding Step}" Height="6" Foreground="{StaticResource AppPrimaryBrush}" />
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,18,0,12">
                    <Grid>
                        <StackPanel Visibility="{Binding IsFirstStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="بيانات الحساب" Style="{StaticResource SectionTitle}" />
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" ToolTip="الاسم الكامل" />
                            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" ToolTip="البريد الإلكتروني" />
                            <PasswordBox x:Name="PasswordInput" PasswordChanged="PasswordChanged" ToolTip="كلمة المرور" />
                            <PasswordBox x:Name="PasswordConfirmationInput" PasswordChanged="PasswordChanged" ToolTip="تأكيد كلمة المرور" />
                            <ComboBox ItemsSource="{Binding Genders}" SelectedItem="{Binding Gender}" ToolTip="الجنس" />
                            <DatePicker SelectedDate="{Binding BirthDate}" />
                            <TextBox Text="{Binding Country}" ToolTip="الدولة" />
                            <TextBox Text="{Binding City}" ToolTip="المدينة" />
                            <TextBox Text="{Binding PhoneZone}" ToolTip="مفتاح الدولة" />
                            <TextBox Text="{Binding Phone}" ToolTip="رقم الهاتف" />
                        </StackPanel>
                        <StackPanel Visibility="{Binding IsSecondStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="وقت الحضور" Style="{StaticResource SectionTitle}" />
                            <TextBox Text="{Binding Timezone}" ToolTip="المنطقة الزمنية" />
                            <ComboBox SelectedValue="{Binding AttendanceDay}" ToolTip="رقم يوم الأسبوع (0–6)">
                                <ComboBoxItem Content="0" /><ComboBoxItem Content="1" /><ComboBoxItem Content="2" /><ComboBoxItem Content="3" /><ComboBoxItem Content="4" /><ComboBoxItem Content="5" /><ComboBoxItem Content="6" />
                            </ComboBox>
                            <TextBox Text="{Binding AttendanceFrom}" ToolTip="من (HH:mm)" />
                            <TextBox Text="{Binding AttendanceTo}" ToolTip="إلى (HH:mm)" />
                            <TextBox Text="{Binding TeacherCode}" ToolTip="رمز المعلم (اختياري)" />
                        </StackPanel>
                        <StackPanel Visibility="{Binding IsThirdStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="خطة المتابعة" Style="{StaticResource SectionTitle}" />
                            <ComboBox ItemsSource="{Binding Frequencies}" SelectedItem="{Binding Frequency}" ToolTip="التكرار" />
                            <ComboBox ItemsSource="{Binding TaskTypes}" SelectedItem="{Binding TaskType}" ToolTip="نوع المهمة" />
                            <TextBox Text="{Binding PlanUnit}" ToolTip="الوحدة: page أو juz أو hizb" />
                            <TextBox Text="{Binding PlanAmount}" ToolTip="الكمية" />
                            <TextBlock Text="{Binding Message}" Style="{StaticResource BodyMuted}" />
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
                <DockPanel DockPanel.Dock="Bottom">
                    <Button Content="السابق" Command="{Binding PreviousCommand}" Style="{StaticResource AppSecondaryButton}" DockPanel.Dock="Right" />
                    <Button Content="التالي" Command="{Binding NextCommand}" DockPanel.Dock="Right" />
                    <Button Content="إنشاء الحساب" Command="{Binding SubmitCommand}" />
                </DockPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
