<UserControl x:Class="Halaqa.Desktop.Features.Auth.Presentation.Views.TeacherRegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <Grid Background="{StaticResource AppCanvasBrush}">
        <Border Style="{StaticResource AppCard}" Width="760" MaxHeight="720" HorizontalAlignment="Center" VerticalAlignment="Center">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="إنشاء حساب معلم" Style="{StaticResource PageTitle}" />
                    <TextBlock Text="شارك بياناتك المهنية لتبدأ إدارة حلقاتك من مكان واحد." Style="{StaticResource BodyMuted}" Margin="0,4,0,16" />
                    <ProgressBar Minimum="1" Maximum="2" Value="{Binding Step}" Height="6" Foreground="{StaticResource AppPrimaryBrush}" />
                </StackPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,18,0,12">
                    <Grid>
                        <StackPanel Visibility="{Binding IsFirstStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="بيانات الحساب والتواصل" Style="{StaticResource SectionTitle}" />
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="الاسم الكامل" />
                            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="البريد الإلكتروني" />
                            <PasswordBox x:Name="PasswordInput" PasswordChanged="PasswordChanged" materialDesign:HintAssist.Hint="كلمة المرور" />
                            <PasswordBox x:Name="PasswordConfirmationInput" PasswordChanged="PasswordChanged" materialDesign:HintAssist.Hint="تأكيد كلمة المرور" />
                            <ComboBox ItemsSource="{Binding Genders}" SelectedItem="{Binding Gender}" materialDesign:HintAssist.Hint="الجنس" />
                            <DatePicker SelectedDate="{Binding BirthDate}" />
                            <TextBox Text="{Binding Country, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="الدولة" />
                            <TextBox Text="{Binding City, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="المدينة" />
                            <TextBox Text="{Binding PhoneZone, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مفتاح الدولة" />
                            <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="رقم الهاتف" />
                        </StackPanel>
                        <StackPanel Visibility="{Binding IsSecondStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="البيانات المهنية" Style="{StaticResource SectionTitle}" />
                            <TextBox Text="{Binding Qualification, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="المؤهل أو الإجازة" />
                            <TextBox Text="{Binding ExperienceYears, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="سنوات الخبرة" />
                            <TextBox Text="{Binding AvailableTime, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="أوقات التوفر (اختياري)" />
                            <TextBox Text="{Binding MaxHalaqas, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="الحد الأقصى للحلقات (اختياري)" />
                            <TextBox Text="{Binding Bio, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" Height="90" materialDesign:HintAssist.Hint="نبذة تعريفية (اختياري)" />
                            <TextBlock Text="{Binding Message}" Style="{StaticResource BodyMuted}" />
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
                <DockPanel DockPanel.Dock="Bottom">
                    <Button Content="السابق" Command="{Binding PreviousCommand}" Style="{StaticResource AppSecondaryButton}" DockPanel.Dock="Right" />
                    <Button Content="التالي" Command="{Binding NextCommand}" DockPanel.Dock="Right" />
                    <Button Content="إنشاء الحساب" Command="{Binding SubmitCommand}" />
                </DockPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
