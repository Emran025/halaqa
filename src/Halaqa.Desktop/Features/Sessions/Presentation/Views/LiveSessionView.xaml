<UserControl x:Class="Halaqa.Desktop.Features.Sessions.Presentation.Views.LiveSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">
    <Grid Background="{StaticResource AppCanvasBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Padding="18,12" Background="{StaticResource AppSurfaceBrush}" BorderBrush="#14000000" BorderThickness="0,0,0,1">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="VideoOutline" Width="22" Height="22" Foreground="{StaticResource AppInfoBrush}" />
                    <TextBlock Text="الجلسة المباشرة" Style="{StaticResource SectionTitle}" Margin="10,0,0,0" />
                    <TextBlock Text="{Binding Store.ConnectionState}" Style="{StaticResource BodyMuted}" Margin="14,3,0,0" />
                </StackPanel>
                <TextBlock DockPanel.Dock="Left" Text="{Binding Store.ConnectionMessage}" Foreground="{StaticResource AppWarningBrush}" VerticalAlignment="Center" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource AppSurfaceBrush}" CornerRadius="12" Padding="16" Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Text="المهمة الحالية" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="يظهر هنا نطاق التلاوة، حالة المهمة، والأخطاء والملاحظات المرتبطة بها." Style="{StaticResource BodyMuted}" Margin="0,10,0,16" />
                    <Button Command="{Binding RequestRepeatCommand}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="Replay" Width="18" Height="18" Margin="0,0,8,0" />
                            <TextBlock Text="طلب إعادة التلاوة" Foreground="White" />
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding SaveOfficialMushafStateCommand}" Style="{StaticResource MaterialDesignOutlinedButton}">
                        <TextBlock Text="تثبيت موضع المصحف" />
                    </Button>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1" Margin="0,0,12,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.32*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>

                <Border Grid.Row="0" Grid.ColumnSpan="2" Background="#FF17201D" CornerRadius="12" Padding="18" Margin="0,0,0,12">
                    <Grid>
                        <TextBlock Text="فيديو الطرف الآخر — مسار P2P مباشر" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <Border Width="160" Height="100" HorizontalAlignment="Left" VerticalAlignment="Bottom" Background="#FF34433C" CornerRadius="8" Padding="8">
                            <TextBlock Text="المعاينة المحلية" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Grid.ColumnSpan="2" Background="{StaticResource AppSurfaceBrush}" CornerRadius="12" Padding="24">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock Text="المصحف التفاعلي" Style="{StaticResource PageTitle}" HorizontalAlignment="Center" />
                        <TextBlock Text="تغييرات الصفحة والآية والمؤشر تظهر مباشرة للطرف الآخر عبر DataChannel، ثم تثبت رسمياً عند الطلب." Style="{StaticResource BodyMuted}" Margin="0,10,0,16" TextAlignment="Center" />
                        <Button Command="{Binding PublishMushafPresenceCommand}" HorizontalAlignment="Center">
                            <TextBlock Text="مشاركة موضع المصحف" Foreground="White" />
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Column="2" Background="{StaticResource AppSurfaceBrush}" CornerRadius="12" Padding="16">
                <StackPanel>
                    <TextBlock Text="حالة الطرف الآخر" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="{Binding Store.PeerMushafPresence.PageNumber, StringFormat=الصفحة: {0}}" Style="{StaticResource BodyMuted}" Margin="0,10,0,0" />
                    <TextBlock Text="{Binding Store.PeerMushafPresence.AyahId, StringFormat=الآية: {0}}" Style="{StaticResource BodyMuted}" />
                    <Separator Margin="0,16" />
                    <TextBlock Text="التسجيل المحلي" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="لا يرفع التسجيل ولا يشارك تلقائياً مع الخادم أو الطرف الآخر." Style="{StaticResource BodyMuted}" Margin="0,8,0,8" />
                    <Button Command="{Binding ToggleLocalRecordingCommand}" Style="{StaticResource MaterialDesignOutlinedButton}">
                        <TextBlock Text="بدء / إيقاف التسجيل المحلي" />
                    </Button>
                    <TextBlock Text="{Binding Store.Recording.State}" Style="{StaticResource BodyMuted}" Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource AppSurfaceBrush}" Padding="14" BorderBrush="#14000000" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding ToggleMicrophoneCommand}" ToolTip="كتم أو تشغيل الميكروفون">
                    <materialDesign:PackIcon Kind="MicrophoneOutline" Width="22" Height="22" />
                </Button>
                <Button Command="{Binding ToggleCameraCommand}" ToolTip="تشغيل أو إيقاف الكاميرا">
                    <materialDesign:PackIcon Kind="VideoOutline" Width="22" Height="22" />
                </Button>
                <Button Command="{Binding ToggleLocalRecordingCommand}" ToolTip="تسجيل محلي">
                    <materialDesign:PackIcon Kind="RecordCircleOutline" Width="22" Height="22" />
                </Button>
                <Button Background="{StaticResource AppErrorBrush}" ToolTip="مغادرة الجلسة">
                    <materialDesign:PackIcon Kind="PhoneHangup" Width="22" Height="22" Foreground="White" />
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
